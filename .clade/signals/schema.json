{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/YuDefine/clade/blob/main/vendor/signals/schema.json",
  "title": "clade improvement-loop signal record",
  "description": "JSONL line schema for improvement-loop signal records emitted by shims, wrappers, git hooks, agent hooks, and manual entries. Every persisted record MUST validate; the validator enforces non-bypassable redaction. $defs.flow_envelope extends the same governance (same validator module, same non-bypassable redaction) to flow-spine events in .clade/flow/events.jsonl; it is a second record shape inside this one schema file, never a separate schema.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "consumer_id",
    "repo_id",
    "event_id",
    "ts_utc",
    "session_id",
    "gate_name",
    "command_fingerprint",
    "error_fingerprint",
    "severity",
    "redaction_applied",
    "source",
    "commit_sha",
    "branch"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1"
    },
    "consumer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable id from registry/consumers.json. Digest aggregation resolves identity through the registry, never through local paths."
    },
    "repo_id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$",
      "description": "owner/repo form, e.g. <client-a>/<consumer-i>."
    },
    "event_id": {
      "type": "string",
      "format": "uuid",
      "minLength": 36,
      "maxLength": 36
    },
    "ts_utc": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 UTC timestamp. Local time is rendered only at display, never stored."
    },
    "session_id": {
      "type": "string",
      "minLength": 1,
      "description": "Source-specific session identifier (shim pid, agent session id, hook invocation id)."
    },
    "gate_name": {
      "type": "string",
      "enum": [
        "vp-check",
        "vp-lint",
        "vp-fmt",
        "validate-manifests",
        "publish",
        "propagate",
        "pre-commit",
        "pnpm-test",
        "pnpm-lint",
        "pnpm-typecheck",
        "audit-script",
        "review-output",
        "other"
      ]
    },
    "command_fingerprint": {
      "type": "string",
      "minLength": 1,
      "description": "Normalized argv joined by ':' with stable ordering; user paths replaced by symbolic tokens. Used for cross-event grouping."
    },
    "error_fingerprint": {
      "type": "string",
      "minLength": 1,
      "description": "Normalized top stack frame, rule code, or error key. Empty failures use the literal string 'unknown'."
    },
    "severity": {
      "type": "string",
      "enum": ["P0", "P1", "P2"]
    },
    "redaction_applied": {
      "type": "boolean",
      "const": true,
      "description": "Non-bypassable. The validator rejects records where this is false; there is no opt-out flag at any level."
    },
    "source": {
      "type": "string",
      "enum": ["shim", "repo-script", "git-hook", "post-tool-use", "manual"]
    },
    "commit_sha": {
      "type": "string",
      "pattern": "^([0-9a-f]{7,40}|unknown)$"
    },
    "branch": {
      "type": "string",
      "minLength": 1
    }
  },
  "$defs": {
    "flow_envelope": {
      "title": "clade flow-spine event envelope",
      "description": "Append-only event on the cross-substrate spine (.clade/flow/events.jsonl). Correlates work across pi / codex / herdr / work-loop through a shared work_id, with OTel-shaped span_id / parent_span. Validated by the same validator module and the same non-bypassable redaction as the signal record above.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "event_id",
        "work_id",
        "span_id",
        "parent_span",
        "phase",
        "kind",
        "actor",
        "substrate",
        "ts_utc",
        "consumer_id",
        "repo_id",
        "session_id",
        "payload",
        "outcome",
        "redaction_applied"
      ],
      "properties": {
        "schema_version": {
          "type": "string",
          "const": "1"
        },
        "event_id": {
          "type": "string",
          "format": "uuid",
          "minLength": 36,
          "maxLength": 36
        },
        "work_id": {
          "type": "string",
          "pattern": "^W-[0-9]{4}-[0-9]{2}-[0-9]{2}-[a-z0-9][a-z0-9-]*$",
          "description": "W-<YYYY-MM-DD>-<slug>, minted by `flow open <slug>`. Dispatches with no ambient work_id mint an orphan one (slug prefixed `orphan-`) so the event still joins the stream."
        },
        "span_id": {
          "type": "string",
          "pattern": "^[0-9a-f]{16}$",
          "description": "OTel-shaped 8-byte span id, lowercase hex."
        },
        "parent_span": {
          "type": ["string", "null"],
          "pattern": "^[0-9a-f]{16}$",
          "description": "Parent span id, or null for a root span. Read from CLADE_FLOW_PARENT_SPAN when a dispatch runs inside another span."
        },
        "phase": {
          "type": "string",
          "enum": ["start", "end", "point"],
          "description": "start/end pair one span; point is an instantaneous event (e.g. work.open). An unmatched start is an in-flight or stalled span, which is a reportable state, not a defect."
        },
        "kind": {
          "type": "string",
          "enum": [
            "work.open",
            "invoke_agent",
            "invoke_workflow",
            "execute_tool",
            "plan",
            "session_transport",
            "gate",
            "decision.request",
            "decision.clarify",
            "other"
          ],
          "description": "OTel GenAI semconv gen_ai.operation.name vocabulary, plus work.open for the work-item lifecycle point event and decision.request for the human-decision queue. decision.request is a SPAN, not a point: phase=start is the question, phase=end is the answer, so an unanswered decision is an unclosed span and every in-flight / stalled query already covers it. There is deliberately no separate decision.resolve kind — it would be dead vocabulary that nothing reads, and the resolved-ness already lives in phase. Not OTel vocabulary; dotted like work.open to mark it as clade's own lifecycle, so an OTLP exporter maps it to a span event and NEVER to gen_ai.operation.name. It exists because the spine records what happened while nothing recorded what is waiting on a human — the \\my contract states outright that pending decisions 'mostly exist only in the conversation', which no downstream can query. decision.clarify is a POINT event hanging off a decision.request span (parent_span = the decision), payload.direction = request | response — the back-and-forth on a question that was asked too tersely to answer. It is NOT the decision.resolve this same paragraph rules out: resolve would have restated a fact phase already carries, while a clarification is a fact nothing else records, and it deliberately does NOT close the span — the human still has to answer. Both halves share one kind because every reader wants them as one ordered list; two kinds would make the common query a union of two filters, and a reader that forgot one half would silently render half a conversation."
        },
        "actor": {
          "type": "string",
          "minLength": 1,
          "description": "Who acted: model slug, human id, or script name."
        },
        "substrate": {
          "type": "string",
          "enum": [
            "pi",
            "codex",
            "herdr",
            "work-loop",
            "harness-workflow",
            "claude-code",
            "git",
            "ci",
            "propagate",
            "manual"
          ],
          "description": "Execution substrate the span ran on. New substrates are added here, never by widening a payload field."
        },
        "ts_utc": {
          "type": "string",
          "format": "date-time",
          "description": "RFC3339 UTC. Local time is rendered only at display, never stored."
        },
        "consumer_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable id from registry/consumers.json."
        },
        "repo_id": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$|^unknown/unknown$"
        },
        "session_id": {
          "type": "string",
          "minLength": 1
        },
        "payload": {
          "type": "object",
          "description": "Substrate-specific detail. Redaction recurses into it; nested values are redacted like top-level ones."
        },
        "outcome": {
          "type": ["string", "null"],
          "enum": ["ok", "fail", "blocked", "skipped", null],
          "description": "Settled result on a phase=end/point event; null while in flight (phase=start)."
        },
        "redaction_applied": {
          "type": "boolean",
          "const": true,
          "description": "Non-bypassable, same contract as the signal record: the validator rejects false; there is no opt-out at any level."
        }
      }
    }
  }
}
