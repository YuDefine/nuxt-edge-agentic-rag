# TD-912: `scripts.tag` 把 tag 推在 main 之前

## 訊號

`package.json` 的 `scripts.tag` 逐字含 `&& git push origin --tags`，會被 `tag-position` gate 擋下、
`&&` 整條中止 → main 與 tag 都不落地（TD-906 死鎖）。同批點名的 perno /
`yuntech-usr-sroi` / `nuxt-supabase-starter` 三家已各自修掉。

## 已完成（2026-09-06 by pane w1V:p1N）

`package.json:54` 改為：

```
"tag": "git tag v$(node -p \"require('./package.json').version\")",
```

驗收已跑，通過：

```
$ node -p "require('./package.json').scripts.tag"
git tag v$(node -p "require('./package.json').version")
```

不含 `git push`。與 perno 現況逐字一致。

## 殘工：這一行還沒 commit

**檔案所有權**：`package.json` 這一行是本 task 的，工作區其餘 ~30 個 dirty 檔屬 pane `w1V:p1C`
（staging 部署 + evlog map coverage gate），該 pane 當時正越過 hard tier 做 relay 交棒。

**為什麼沒 commit**：

- `package.json` 不在 `commit.detail.md` § `--only` 適用範圍 的路徑白名單，
  `pre-bash-git-commit-only-whitelist.sh` 在 main 上擋下 ad-hoc 提交（實測擋過一次）。
- 白名單外一律走 `/commit`，而 `/commit` 決策樹是全包 —— 那會把 p1C 交棒中的 WIP
  一起收進來，超出本次 dispatch 的範圍。

**已做的協調**：已用 `herdr agent prompt w1V:p1C` 告知這一行的存在與驗收結果，
請它帶進 relay brief。

**剩餘步驟**：下一個在本 repo 跑 `/commit` 的 session，確認 `package.json:54` 這一行
仍是上面的形狀後隨批次一起落地即可。無其他相依。
