# Handoff

## In Progress

- 目前沒有待接手的 in-progress Spectra change。

## Next Steps

1. `openspec/changes/` 目前只剩 `archive/`；`integrate-workers-ai-answer-and-judge` 與 `implement-web-chat-sse-streaming` 已封存。
2. 若要開始新工作，先建立或取回對應 Spectra change，再更新本檔 handoff。
