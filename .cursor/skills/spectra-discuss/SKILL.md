---
name: spectra-discuss
description: 'spectra-discuss 相容入口；以 OPSX inquiry 的 discuss 模式完成唯讀調查。'
effort: high
permission_tier: read-only
---
<!--
🔒 LOCKED — managed by clade
Source: plugins/hub-core/skills/spectra-discuss/
Edit at: $CLADE_HOME
Local edits will be reverted by the next sync.
-->


# 接續 OPSX

此入口保留舊名稱相容性。讀取同 plugin 的 `../opsx/SKILL.md`，以 `discuss` 模式讀 `../opsx/references/inquiry.md` 並完成原請求；保留唯讀範圍，調查結果不自動轉成需求 intake。

每一個舊入口都只走 OPSX／flow 的共同 command。NEVER 啟動 Spectra binary，或直接改 tasks checkbox、touched sidecar、歷史 artifact 來宣告完成。歷史內容由 OPSX history 讀取；尚未承接的需求明示未遷移，保留原件與來源，不把封存原件當作需求完成。
