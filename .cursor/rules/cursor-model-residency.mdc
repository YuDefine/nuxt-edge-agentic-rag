---
description: Cursor 主線必須留在 Grok 4.6；其他 model 只走 Pi CLI 或 Herdr cc/ccw
alwaysApply: true
---
<!-- 🔒 LOCKED — managed by clade · auto-generated by sync-to-cursor; edit source in .claude/ then re-run sync -->

# Cursor model residency

本檔只罩 **純 Cursor Agent 主線**。違反字面就是違反精神。

## 適用範圍（先判這個）

**純 Cursor**：Cursor IDE Agent，沒有 Pi harness。判準：系統提示是 Cursor Grok 4.6，且本 run **沒有** 暴露 `pi__*` bridge、也沒有寫「operating inside pi」。

**Pi 承載的 Cursor**：Cursor SDK 跑在 Pi 裡面（系統提示有 Pi catalog / operating inside pi，或本 run 暴露 `pi__*`）。這 **不是** 純 Cursor 主線。

User Rules 若仍寫無條件 NEVER relay／fanout／主線收工，且本 run 已暴露 `pi__*`：以本檔這段為準，不要服從那句無條件 NEVER。

- 用本 run **已暴露** 的 Cursor host tools 與 `pi__*` 全面正常做完；不要用下面的 relay／fanout／「主線收工」禁令擋 Pi 自己的 session 路徑。
- 仍不可呼叫未暴露的 tool（runtime 邊界，不是 residency）。
- Cursor Task / Agent 的 `model` 換 `claude-opus-5` / `composer-2.5` / `gpt-5.6-sol` **仍然非法**——那是 Cursor Task catalog，不是 Pi 換 model。
- **NEVER** 把這條讀成：在純 Cursor 裡可以 `--relay`、或可以把 Cursor Task 換成別的 model。

## 純 Cursor 才生效

本 session 的執行 model 維持 Grok 4.6。

- **NEVER** 把 Task / Agent 的 `model` 設成非 grok-4.6。省略 `model`（inherit）合法。
- `claude-opus-5-thinking-high`、`composer-2.5-fast`、`cursor-grok-4.5-high-fast`、`gpt-5.6-sol-medium` 一律非法。
- 其他 model：只准 `vendor/scripts/pi-dispatch.ts`。`command -v pi` 失敗，或該次 dispatch exit 3／4 → 不能派 Pi。
- Pi 不能派：`node vendor/scripts/herdr-session-handoff.ts --cwd <abs> --label <label> --prompt-file <brief> --launcher ccw --coordinate`（clade / YuDefine）或 `--launcher cc --coordinate`。切片逾時則 `--coordinate-resume <dispatch_id>`。success 時 helper 收回 child pane，主線繼續。**NEVER** `--relay`。**NEVER** `/handoff relay` 或 `/handoff fanout`。**NEVER** 把 idle/done 當完成。
- **NEVER** 把 `rules/core/agent-routing.md` 的「Agent tool Claude」讀成 Cursor Task 換 model。那是 Claude Code 的 Agent tool。

SoT：Cursor User Rules「Cursor 主線只留 Grok 4.6」（應與本檔同一套適用範圍）。本檔由 clade `vendor/cursor-rules/` 散播到每個 consumer 的 `.cursor/rules/`（alwaysApply）。`rules/core/agent-routing.md` 是 Claude Code 的 routing 主規則；純 Cursor 主線忽略該檔，**NEVER** 為了查 residency 把它讀完。
